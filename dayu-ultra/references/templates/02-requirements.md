# <项目名>：需求与验收门槛

> 日期：<日期>；版本：<版本>（<阶段后缀>）
> 状态：<已批准的部分>；<仍缺什么>；产品能力仍待验证。
> **本文不是已实现的软件说明。本轮没有编写代码、没有安装、没有部署。**
> 配套：<链接>｜方法论：Agile + Evolutionary Design + Selective TDD

## 1. 目标与可行性结论

<一段话说清要做什么、为什么可行、前提是什么>

## 2. 已确认需求

> 以下均来自用户明确选择。后文带"（拟定）"的内容属于待审阅的工程方案，
> **不能反向视为用户已经批准**。

| 编号 | 已确认要求 |
| --- | --- |
| `R01` | <对象 + 行为 + 边界/禁止项；一行一条；不写数值门槛> |
| `R02` | |

写法：写行为不写实现；**把禁止项写进需求**（"不覆盖、不自动提交"）；不写数值门槛。

## 3. 验收门槛

> 门槛承载数值与判定标准；需求只写意图。门槛跨增量，**不要把隐私/异常/集成拖到最后**。

| 门槛 | 判定标准 |
| --- | --- |
| `G1` | <含样本量、成功条件、时限，可判定> |

### 3.1 测量口径

- 样本量：<每组多少次>
- **分母**：失败/超时/异常**不剔除**
- 分组：冷启动 / 稳定态 / 重连 / 主动取消**分别报告**
- P50/P95 为描述统计，**不替代硬门槛**

## 4. 架构候选与推荐（拟定）

| 方案 | 优点 | 约束与结论 |
| --- | --- | --- |
| A | <优点> | <是否被选 / 为何 / **失败时禁止自动降级到什么**> |
| B | | <未选需保留比较记录，切换需重新评审> |
| C | | <不满足需求的原因> |

> 排除清单：<明确不在本期范围的做法>
> 若方案 A 的关键门槛失败，应**重新提交设计选择**，而不是自动退到方案 C。

## 5. 待审定事项与停止条件

<逐条列出：待定什么 / 为什么不能自己定 / 失败时怎么办>

**停止条件**（不是项目终止，而是"停止推进、回到上一级重新决策"）：

| 层 | 停止条件 |
| --- | --- |
| 方案层 | 关键门槛失败 → 重新提交设计选择，**不自动降级到曾被否决的方案** |
| 发布层 | 未通过门槛不发布完整版，**也不用替代手段绕过** |
| 指标层 | 未达标 → 报告数据并提出选择，**不擅自降低能力或改口径** |

> **设计批准不等于完成实机验证。** 以下验证只能在获得编码/测试授权后实施；当前只定义判定条件。
