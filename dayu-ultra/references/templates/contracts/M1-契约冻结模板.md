# M<N> <名称>契约冻结

> 状态：**M<N> 开工冻结（工程取值已于 <日期> 获用户批准）**
> 依据：<C/ADR 编号>、<HLD 章节>、<测试矩阵章节>
> 冻结日期：<日期>
> **本文件解除 <约束ID>「<原文描述的开工阻塞>」的开工阻塞。**
> 第 <N>/<M> 节取值已获用户批准，作为验收基线。

## 1. 冻结范围

| 冻结 | 不冻结（各自里程碑前） |
| --- | --- |
| <本节点必须确定的项> | <先不定的项>（`M<N>` 前，见 `C<NN>`） |

> 右列每一项都必须带"（某里程碑前，见某约束 ID）"。没有时点的"以后再说"等于永远不做。

## 2. <传输与安全边界 / GATT 布局 / …>

| 项 | 冻结值 | 依据 |
| --- | --- | --- |

## 3. <端点 / 包格式 / 接口>

| ID | <方法 / 路径 / 类型> | 成功 | 失败 |
| --- | --- | --- | --- |

### 3.1 <状态 → 结果映射（全接口统一）>

| 状态 | <映射> | 含义 | 客户端允许动作 |
| --- | --- | --- | --- |

> 若涉及结果判定，写明：**必须以 <状态字段> 判定结果；除 <明确成功态> 外一律不得当作成功。**

## 4. <状态机与线性化点>

```
<状态图>
```

- **线性化点**：<组件内><同步完成><某状态变更并记录结果>的那一步；
  **不是** <候选时点 A>、**不是** <候选时点 B>。
- <按"谁先到达线性化点"穷举各顺序的结果>
- <明确声明**不提供**的保证>

## 5. <容量与期限（已批准值，日期）>

| 符号 | 批准值 | 单位 | 说明 |
| --- | --- | --- | --- |

## 6. 明确不做

- 不提供 <能力>。
- 不使用 <已知不安全的替代路径>。
- 不把 <被排除的接口> 暴露出去。

## 7. 已批准项（<日期> 用户确认）

| 项 | 批准值 | 影响 |
| --- | --- | --- |

## 8. 变更规则

任何后续变更须：**更新本文件版本 → 同步契约测试与受影响回归 → 记录 ADR → 不保留未发布版本的兼容包袱。**
