# 开发准备就绪核查

> 日期：<日期>｜范围：<为哪些节点做准备>
> **本文件是准备状态报告，不是功能交付报告。** <哪些节点仍未开始>。

## 1. 结论摘要

| 领域 | 状态 |
| --- | --- |
| 设计文档 | `✅` 完整 / `⏳` 部分 / `⚠️` 含未审查项 |
| 仓库骨架 | |
| 契约冻结 | |
| 测试基础设施 | |
| 许可证 | |
| 用户决策项 | |

> 三态符号：`✅` 完整/已冻结/已验证/已批准｜`⏳` 骨架就绪/清单就绪/未开始｜`⚠️` 部分完成且含未审查项

## 2. 工具链环境（已核实）

| 模块 | 工具 | 版本 | **验证方式** |
| --- | --- | --- | --- |
| | | | <实跑 / exit 0 / 实编译链接运行 / **可用（未走此路径）**> |

> "验证方式"列必须绑定**可复现动作**或**产物哈希**。
> 未走通的旁支也要显式写出，不能只说"可用"。

## 3. 仓库骨架

```
<目录树 + 用途注释>
```

## 4. 契约冻结状态

| 契约 | 状态 | 说明 |
| --- | --- | --- |
| | `✅ 已冻结` / `⏳ 清单就绪` / `⏳ 未开始` | <含量化待办，如"N 项待填值；M<N> 前冻结"> |

## 5. 准备过程中发现并修正的事实

| 发现 | 影响 | 处理 |
| --- | --- | --- |
| <改变了设计/契约事实的发现> | <影响到什么> | <**回写了哪份上游文档**；转成了哪条用例证据要求> |

> 纪律：发现必须**回写上游产物**、**转换成用例证据要求**、**与既有基线冲突时显式挂待确认**。

## 6. 环境故障根因与修复记录

**根因**：1. <…> 2. <…>

**修复**：1. <…>

**验证结果**：<真实输出、版本、产物大小、哈希>

**固化为单一入口**：<脚本路径 —— 把版本/路径/环境变量封装在一处>

## 7. 已批准项（<日期> 用户确认）

| 项 | 批准值 | 说明 |
| --- | --- | --- |
| | | |

> **回写声明**：上述取值已写入 <契约文档> 第 N 节，**作为验收基线**。

## 8. 仍未就绪（是否阻塞下一阶段）

- <项> —— **不阻塞 `M<N>` 开工** / **阻塞 `M<N>`**
- 边界：<未执行任何产品测试；无设备端验证>
